on:
  schedule:
    - cron:  '10 3 * * 6'
    
  workflow_dispatch:

name: generate-translations

jobs:
  generate-translations:
    runs-on: ubuntu-latest
    env:
      REPO_KEY: ${{ secrets.REPOS_KEY }}
      WEBLATE_KEY: ${{ secrets.WEBLATE_KEY }}
      GH_TOKEN: ${{ github.token }}
      QML_REPO: jaspdescriptives-qml
      R_REPO: jaspdescriptives-r

    steps:
      #- uses: jasp-stats/jasp-actions/wlcLock@master

      - uses: shun2wang/jasp-actions/translations@fixTr

